# Time-series forecasting (Chronos-2 shadow signal)
chronos-forecasting>=2.0,<3

# Time-series forecasting (TimesFM-3 shadow signal, side-by-side with Chronos).
# 3.0.0 is the first release exposing the timesfm3 PyTorch API; pin major so a
# future 4.x can't silently change ForecastOutput shapes. Weights under the
# non-commercial license — module ships disabled by default.
timesfm[torch]==3.0.0

# Dashboard + operator console (python -m hermes_trader.server)
fastapi>=0.141
uvicorn>=0.52
prometheus-client>=0.26
python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.0
httpx>=0.27.0
pydantic>=2.0.0
rich>=13.7.0
pyfiglet>=1.0.2
certifi>=2023.5.0
eth-account>=0.8.0
hyperliquid-python-sdk>=0.24.0
httpcore>=1.0.0